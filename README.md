# Maze Runner Robot

PlatformIO project: Maze Runner Robot

This repository contains code for the Maze Runner Robot project (odometry testing on embedded hardware).

## Structure

- `src/` — source files
- `include/` — headers
- `lib/` — libraries
- `test/` — tests

## Usage

Open the project with PlatformIO (VS Code) and build/upload to your board.
